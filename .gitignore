# Excel 共享表（精工业务数据，不入 git）
*.xlsx
*.xls
*.bak.*
.~*.xlsx

# Python 缓存
__pycache__/
*.py[cod]
*$py.class
*.so

# macOS
.DS_Store
._*

# 临时文件
*.tmp
*.swp
*~

# 日志
*.log
logs/

# 数据/输出目录（看板 HTML 需入 git 供 GitHub Pages 部署）
data/
output/*
!output/看板.html

# 个人备份
*.bak
*~

# 内部业务文档（可能含真实价格数据，不传公网）
*.pdf
核查报告*.md

# 每日抓取的当日数据归档（含 ccmn 真实价格，不传公网）
长城有色日价格查询/
# 2026-09-23：该归档已迁至 sources/精工/上海有色日价格查询/。
# 原规则只匹配旧目录名（长城有色日价格查询），新位置会漏网，故补一条。
# 注：其中 7 个文件（2026-07-13 ~ 07-21）历史上已被跟踪，保持现状不动；
# 后续新生成的归档文件不再进入版本管理。
sources/精工/上海有色日价格查询/

# 每日抓取截图（含亚洲金属网/SMM 登录态截图，严禁入 git）
screenshots/

# 凭据文件（含 SMM / 亚洲金属网账号密码，严禁入 git）
.env
.env.local
.env.*.local
secrets.json
credentials.json

# 浏览器调试缓存（可能含登录态/cookie，严禁入 git）
.chrome-debug-profile/

# 导出的第三方站点 cookie（含卓创会员会话凭证，严禁入 git）
cookies/
.workbuddy/
.codebuddy/
.trae-html-share-packages/
dist/

# 临时分析产物
jinggong-eval-*/
项目评价报告*.html

# 第三方参考资料（客户提供的原始图表/报告，含对方业务数据，严禁入 git）
references/

# 仓库整理新增（2026-09-23）
# 备份归档（含主共享表快照与看板 json 快照，属本地回滚用途）
backups/
# 归档区（OCR 临时切片、看板原型副本、一次性维护脚本）
_archive/
